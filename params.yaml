base:
  random_seed: 42

data_split:
  test_pct: 0.15

train:
  valid_pct: 0.1
  arch: resnet34
  img_size: 256
  batch_size: 8
  fine_tune_args:
    epochs: 10
    base_lr: 0.003

evaluate:
  n_samples_to_save: 10
